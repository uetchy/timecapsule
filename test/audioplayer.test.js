const { expect } = require('chai')

const { AudioPlayer } = require('../dist/util/audioplayer')

describe('AudioPlayer', function() {
  this.timeout(5000)

  let deltaTime = 0

  it('play', done => {
    const player = new AudioPlayer()
  })
})
